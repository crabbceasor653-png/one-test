# 2026-06-02 开发日志

## 今日目标

- 建立项目开发规划、标准文档和开发日志机制。
- 明确第一版纯前端植被拼贴编辑器的稳定推进步骤。

## 已完成

- 创建 `docs/` 项目文档目录。
- 创建 `dev-logs/` 开发日志目录。
- 创建每日开发日志模板。
- 创建当天开发日志。
- 梳理产品需求、技术架构、设计规范、核心数据结构和分阶段开发计划。
- 创建 `CLAUDE.md`，作为后续开发工作的入口说明。
- 创建 Vite React TypeScript 项目骨架文件。
- 创建 `src/` 基础目录结构：`components/`、`types/`、`utils/`、`store/`。
- 按参考 UI 拆分并实现第一版静态布局组件：顶部栏、素材栏、中央画布区域、右侧图层/属性面板、底部状态栏。
- 编写暗色编辑器主题样式，使用绿色作为主强调色，初步贴近参考图视觉方向。
- 创建 `preview.html` 静态预览页，用于在依赖未安装时验证阶段 0 UI 视觉骨架。
- 创建 `scripts/preview-server.mjs` 本地预览服务器脚本。
- 创建 `scripts/verify-preview.mjs` 自包含验证脚本，用于临时启动服务器并验证预览页和样式文件可访问。
- 依赖安装问题已由用户侧解决，项目恢复正常构建。
- 完成阶段 1：PNG 上传与素材栏真实缩略图展示。
- 新增 `src/utils/image.ts`，用于读取 PNG Data URL 与图片尺寸。
- `AssetPanel` 接入真实素材状态，支持点击上传和拖拽上传。
- 上传素材会过滤非 PNG 文件，并在素材栏显示被忽略文件数量。
- 新增 `.gitignore`，忽略 `node_modules/`、`dist/`、TypeScript build info 和本地验证日志。
- 调整 TypeScript 配置，避免构建后在根目录生成 `vite.config.js` / `vite.config.d.ts`。
- 创建 `start-dev.bat`，用于在 Windows 上一键启动 Vite 开发服务器。
- 完成阶段 2：画布放置与基础选择。
- 新增 `CollageElement` 数据结构，用于描述画布中的图片元素。
- `App.tsx` 新增画布元素状态和当前选中元素状态。
- 上传后的 PNG 素材卡片支持点击添加到画布。
- `CanvasWorkspace` 接入 `react-konva`，创建 Konva Stage 并渲染画布图片。
- 点击画布图片后可设置选中状态，并显示 Konva Transformer 选框。
- 有真实画布元素时隐藏原静态示例选框，避免误导。

## 验证结果

- 已完成规划和规范文件创建。
- 已创建 React 应用代码骨架和第一版静态 UI。
- 已确认本机 Node 版本为 `v24.15.0`，npm 版本为 `11.12.1`。
- 早前 `npm install` 在沙盒内超时，后续依赖安装问题已由用户侧解决。
- 当前 `node_modules/`、`package-lock.json` 和 `dist/` 已存在。
- 运行 `node scripts/verify-preview.mjs` 成功，验证结果：`preview.html 200`、`styles.css 200`。
- 尝试打开本地 `preview.html` 到浏览器窗口，GUI 权限审核未在截止时间内返回。
- 阶段 1 开发前执行 `npm run build`，构建通过。
- 阶段 1 开发后再次执行 `npm run build`，构建通过。
- 使用 PowerShell Job 临时启动 Vite dev server，并请求 `http://127.0.0.1:5173/`，验证结果：`vite dev http 200`。
- 前台执行 `npm run dev -- --host 127.0.0.1 --port 5173` 可正常启动并输出本地访问地址。
- 尝试将 Vite dev server 作为隐藏后台进程长期保留，但当前工具环境会很快回收该进程；未继续阻塞功能开发。
- 尝试使用 Playwright 做 headless 截图验证，但 bundled runtime 缺少对应浏览器二进制，未继续阻塞。
- 用户反馈浏览器访问 `127.0.0.1:5173` 显示连接被拒绝，确认原因是开发服务器未保持运行。
- 尝试用 Node 静态服务器和 Python 静态服务器作为隐藏后台进程提供页面，但当前工具环境同样会回收进程。
- 尝试请求权限打开可见终端运行 `start-dev.bat`，权限审核未在截止时间内返回。
- 阶段 2 开发后执行 `npm run build`，构建通过。
- 接入 Konva 后 Vite 提示 JS chunk 超过 500 kB；这是体积优化警告，不影响当前运行。
- 使用 PowerShell Job 临时启动 Vite dev server，并请求 `http://127.0.0.1:5173/`，验证结果：`vite dev http 200`。

## 待办

- 阶段 3：实现拖动位置更新。
- 阶段 3：实现缩放尺寸更新。
- 阶段 3：实现旋转角度更新。
- 阶段 3：属性面板同步显示和修改选中元素属性。
- 使用应用前需保持开发服务器终端窗口打开；推荐双击 `start-dev.bat` 或手动运行 `npm run dev -- --host 127.0.0.1 --port 5173`。
- 后续如需完整视觉回归，可安装 Playwright 浏览器二进制或使用 Codex Browser 工具。

## 风险与备注

- 第一版应控制范围，优先保证上传、画布编辑、图层、导出 PNG 路径稳定。
- 多选和组合需要分阶段实现，避免过早引入复杂状态。
- 当前已完成阶段 2，画布已接入 Konva 并支持放置和基础选中；拖动、缩放、旋转仍留到阶段 3。
- PNG 使用 FileReader 转为 Data URL，适合第一版纯前端；素材量很大时后续需要考虑内存管理。
- Konva 引入后生产包体积明显增加，后续如需要可做代码分割或手动 chunk 优化。
